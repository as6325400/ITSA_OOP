#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    double num;
    while(cin >> num){
      cout << fixed << setprecision(1) << num * 1.6 << '\n';
    }
}