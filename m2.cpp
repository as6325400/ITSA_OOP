#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    double n, m;
    while(cin >> n >> m){
      cout << fixed << setprecision(1) << n * m / 2.0 << '\n'; 
    }
}